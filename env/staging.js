/*
 * Staging Environment - demo setup for developers to collaborate on.
 */
module.exports = {
  // Turns on Apache-style request logging.
  REQUEST_LOGS: true,

  // Unknown errors will not have their stack traces logged.
  LOG_ERRORS: false,
};
